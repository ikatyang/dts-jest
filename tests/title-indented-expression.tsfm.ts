{
  {
    {
      {
        // @dts-jest:snapshot
        [].map(
          () => '',
        );
      }
      {
        // @dts-jest:snapshot
        [].map(
      () => '',
        );
      }
      {
        // @dts-jest:snapshot
        [].map(


          () => '',


        );
      }
      {
        // @dts-jest:group
        // @dts-jest:snapshot
        [].map(
          () => '',
        );
      }
    }
  }
}
