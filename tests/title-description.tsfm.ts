export {};

declare const void_to_string: () => string;

// @dts-jest:snapshot
void_to_string(

);

// @dts-jest:snapshot
void_to_string();

// @dts-jest:snapshot optional-description
void_to_string();
