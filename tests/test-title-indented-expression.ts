{
  {
    {
      {
        // @dts-jest
        [].map(
          () => '',
        );
      }
      {
        // @dts-jest
        [].map(
      () => '',
        );
      }
      {
        // @dts-jest
        [].map(


          () => '',


        );
      }
      {
        // @dts-jest:group
        // @dts-jest
        [].map(
          () => '',
        );
      }
    }
  }
}
