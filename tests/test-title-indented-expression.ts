{
  {
    {
      {
        // @dts-jest
        [].map(
          () => '',
        );
      }
      {
        // @dts-jest
        [].map(
      () => '',
        );
      }
      {
        // @dts-jest
        [].map(


          () => '',


        );
      }
    }
  }
}
